name: build-dyprobe

on:
  push:
    branches: [ main, master ]
  workflow_dispatch:

jobs:
  build:
    runs-on: macos-14
    steps:
      - uses: actions/checkout@v4

      - name: Show toolchain
        run: |
          xcodebuild -version
          xcrun --sdk iphoneos --show-sdk-path
          xcrun --sdk iphoneos --show-sdk-version

      - name: Install ldid
        run: |
          brew install ldid

      - name: Build arm64 dylib
        run: |
          set -ex
          SDK="$(xcrun --sdk iphoneos --show-sdk-path)"
          xcrun --sdk iphoneos clang \
            -arch arm64 \
            -isysroot "$SDK" \
            -mios-version-min=14.0 \
            -fobjc-arc \
            -dynamiclib \
            -install_name "@rpath/libAweTrace.dylib" \
            -framework Foundation \
            -framework CoreFoundation \
            -lobjc \
            -O2 \
            -Wno-deprecated-declarations \
            DYProbe.m fishhook.c \
            -o libAweTrace.dylib

      - name: Codesign with ldid (fake-sign)
        run: |
          ldid -S libAweTrace.dylib
          file libAweTrace.dylib
          ls -la libAweTrace.dylib

      - name: Verify Mach-O
        run: |
          otool -hv libAweTrace.dylib
          otool -L libAweTrace.dylib
          dwarfdump --uuid libAweTrace.dylib

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: libAweTrace
          path: libAweTrace.dylib
          if-no-files-found: error
